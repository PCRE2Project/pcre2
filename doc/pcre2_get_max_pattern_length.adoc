= pcre2_get_max_pattern_length(3)
:doctype: manpage
:manname: pcre2_get_max_pattern_length
:manpurpose: get the maximum length of a pattern
:manvolnum: 3
:manmanual: PCRE2
:mansource: PCRE2 10.49-DEV
:docdate: 11 September 2026
:revdate: 11 September 2026

== Synopsis

[source,c,role=prototype]
----
#include <pcre2.h>

int pcre2_get_max_pattern_length(pcre2_compile_context *ccontext,
                                 PCRE2_SIZE *length);
----

== Description

This function gets, from a compile context, the maximum text length (in code
units) of the pattern that can be compiled. The result is zero for success or
const:PCRE2_ERROR_NULL if either argument is const:NULL. The largest value a type:PCRE2_SIZE
variable can hold indicates an unlimited limit.

There is a complete description of the PCRE2 native API in the
xref:pcre2api.adoc[`pcre2api`]
page and a description of the POSIX API in the
xref:pcre2posix.adoc[`pcre2posix`]
page.
