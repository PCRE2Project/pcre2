= pcre2_set_max_varlookbehind(3)
:doctype: manpage
:manname: pcre2_set_max_varlookbehind
:manpurpose: set the maximum match length for a variable-length lookbehind
:manvolnum: 3
:manmanual: PCRE2
:mansource: PCRE2 10.49-DEV
:docdate: 11 August 2023
:revdate: 11 August 2023

== Synopsis

[source,c,role=prototype]
----
#include <pcre2.h>

int pcre2_set_max_varlookbehind(pcre2_compile_context *ccontext,
                                uint32_t limit);
----

== Description

This sets a maximum length for the number of characters matched by a
variable-length lookbehind assertion. The default is set when PCRE2 is built,
with the ultimate default being 255, the same as Perl. Lookbehind assertions
without a bounding length are not supported. The result is always zero.

There is a complete description of the PCRE2 native API in the
xref:pcre2api.adoc[`pcre2api`]
page and a description of the POSIX API in the
xref:pcre2posix.adoc[`pcre2posix`]
page.
